# ReviewLens deps (CPU)

# --- phase 1: ABSA / 챗봇 / 웹 ---
torch            # CPU wheel: pip install torch --index-url https://download.pytorch.org/whl/cpu
transformers
pandas
fastapi
uvicorn
# LLM은 Ollama 로컬 서버 사용 (별도 설치): ABSA=qwen2.5:3b, 챗봇/요약=gemma3:4b

# --- phase 2: 추천 협업필터링 ---
implicit         # ALS CF (CPU 네이티브). LightFM은 py3.12+setuptools에서 빌드 불가라 대체
scipy            # sparse 상호작용 행렬

# --- phase 2: 챗봇 의미검색 (RAG retriever) ---
sentence-transformers   # ko-sroberta(jhgan/ko-sroberta-multitask) 임베딩
faiss-cpu               # 근거 리뷰 의미검색 인덱스

# --- phase 2: 감성 분류기 학습 (linear probing, CPU) ---
scikit-learn            # 로지스틱 회귀 헤드 + 평가
joblib                  # 학습된 분류기 직렬화
